const Main = () => {
  return (
    <main>
      <section>
        <h1>Choose mode</h1>
        <button>Player vs Player</button>
        <button>Player vs Computer</button>
      </section>
    </main>
  );
};

export default Main;
